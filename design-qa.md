**Source visual truth**

- `/Users/huicheng/Desktop/画板备份 24@2x.jpg` — 3840×2160 reference, normalized to 1422×800 for comparison.
- Supplied raster assets in `/Users/huicheng/Desktop/wine网站元素/`.
- Interaction evidence: `/Users/huicheng/Desktop/录屏2026-08-29 14.00.04.mov` — 17.185-second user recording of the home-state jump.

**Implementation evidence**

- `.qa/design-qa-detail-polish-3.png` — 1384×900 browser capture after lowering both mosaic groups, lifting the English support copy, and constraining motto highlights to their original transparent stroke silhouettes.
- `.qa/design-qa-mosaic-center-aligned.png` — 1384×900 browser capture after aligning the left mosaic's visible center to the right mosaic.
- `.qa/design-qa-signature-edge-aligned.png` — 1384×898 browser capture after pixel-edge-aligning the W and Chinese mark, equalizing both support rows, and removing the motto highlight animation.
- `.web-shader-extractor/evidence/screenshots/project-1200x675-a.png` and `project-1200x675-b.png` — two browser-rendered frames proving muted autoplay, continuous subject motion, and transparent WebGL compositing at the exact 1200×675 reference viewport.
- `.web-shader-extractor/evidence/screenshots/reference-vs-project.png` — video 2 reference and the implemented WebGL replay in one comparison image.

- `.qa/design-qa-signature-rule-clearance.png` — normalized 1384×900 browser capture after restoring space between the English support copy and the header rules.
- `.qa/focused-signature-rule-clearance.png` — focused implementation crop of the final signature/rule relationship.
- `.qa/focused-comparison-signature-rule-clearance.jpg` — focused source/implementation comparison in one image.
- `.qa/design-qa-signature-copy-clear.png` — normalized browser capture after moving both English support rows below the supplied Wine raster's visible alpha boundary.
- `.qa/focused-signature-copy-clear.png` — focused signature crop used to inspect the two support rows.
- `.qa/design-qa-mosaic-spacing-glow-fit.png` — normalized 1384×900 browser capture after the latest mosaic alignment and reduced motto-light treatment.
- `.qa/side-by-side-mosaic-spacing-glow-fit.jpg` — normalized source/implementation full-view comparison in one image.
- `.qa/focused-comparison-mosaic-spacing.jpg` — focused source/implementation comparison of the left decoration group.
- `.qa/focused-motto-glow-fit.png` — browser-captured peak frame of the reduced lower green highlight.
- `.qa/design-qa-brackets-mosaic-aligned.png` — browser-rendered 1280×720 capture after the bracket and mosaic correction (browser DPR 2 raw capture).
- `.qa/design-qa-brackets-mosaic-normalized.png` — DPR-normalized 1280×720 implementation used for visual comparison.
- `.qa/side-by-side-brackets-mosaic-aligned.jpg` — normalized full-view source/implementation comparison in one image.
- `.qa/focused-comparison-brackets-mosaic.jpg` — focused source/implementation comparison of the left bracket and mosaic group.
- `.qa/focused-brackets-left.png` and `.qa/focused-brackets-right.png` — browser crops used to inspect both mirrored decoration groups.
- `design-qa-nav-default.png` — 1422×800, home selected.
- `design-qa-nav-works-hover.png` — 1422×800, works selected and home unselected.
- `.qa/side-by-side-nav-final.png` — equal-size reference/implementation comparison.
- `design-qa-nav-aligned-default.png` — default home-selected state after transparent-canvas trimming.
- `design-qa-nav-no-jump.png` — immediate works-selected state after removing state transitions.
- `design-qa-pixel-motion.png` — browser-rendered mosaic animation frame.
- `.qa/side-by-side-pixel-motion.png` — normalized reference and animated implementation frame in one comparison image.
- `design-qa-detail-fixes.png` — 1422×800 browser capture after the logo-spacing, 20px navigation, stable home state, and wine-dot motion changes.
- `.qa/side-by-side-detail-fixes.png` — latest full-view reference/implementation comparison.
- `.qa/focused-header-detail-fixes.png` — 2× focused header comparison for logo spacing and navigation typography.
- `design-qa-header-refinement-final.png` — 1422×800 browser capture after the 14px Chinese-mark scaling, proportional signature restoration, tighter small-copy rows, and 22px tabs.
- `.qa/side-by-side-header-refinement.png` — latest full-view normalized reference/implementation comparison.
- `.qa/focused-header-refinement.png` — 2× focused header comparison used to inspect the W silhouette, small-copy rows, and tab scale.
- `design-qa-brand-wine-ratio.png` — 1422×800 browser capture after restoring the Chinese mark and removing the wine-word stretch.
- `.qa/side-by-side-brand-wine-ratio.png` — latest normalized full-view comparison.
- `.qa/focused-wine-ratio.png` — focused source/implementation comparison of the lower wordmark proportions.
- `design-qa-home-alignment.png` — 1422×800 browser capture after the selected-home optical baseline correction.
- `.qa/side-by-side-home-alignment.png` — latest normalized full-view comparison.
- `.qa/focused-home-alignment.png` — 2× focused navigation comparison for the selected-home baseline.
- `.qa/video-home-jump/contact-sheet.png` — 12-frame contact sheet extracted from the user recording.
- `.qa/video-home-jump/before-state-swap.png` — focused two-state evidence showing the old HTML-text/raster swap.
- `design-qa-home-no-swap.png` — browser-rendered default state after removing the content swap.
- `.qa/side-by-side-home-no-swap.png` — latest normalized reference/implementation comparison.
- `.qa/reference-header-lines.png` — source crop used for pixel-run measurement of the three header rules.
- `design-qa-header-lines.png` — 1422×800 browser capture after restoring the measured three-line structure.
- `.qa/side-by-side-header-lines.png` — latest normalized full-view comparison.
- `.qa/focused-header-lines.png` — 3× focused source/implementation comparison of the header rules.
- `.qa/design-qa-signature-copy-alignment.png` — latest browser-rendered implementation after the support-copy alignment update.
- `.qa/side-by-side-signature-copy-alignment.png` — normalized 1280×720 source/implementation comparison.
- `.qa/focused-signature-copy-alignment.png` — focused source/implementation header comparison.
- `.qa/implementation-signature-detail.png` — enlarged browser crop for checking the support-copy start edge against the W.
- `.qa/design-qa-nav-24px.png` — latest browser-rendered implementation after the navigation-size annotation.
- `.qa/side-by-side-nav-24px.png` — normalized source/implementation comparison for the 24px navigation update.
- `.qa/focused-nav-24px.png` — enlarged source/implementation navigation comparison.
- `.qa/motto-shine-frame-a.png` — browser-rendered frame with the lower green-stroke highlight in motion.
- `.qa/motto-shine-upper-peak.png` — browser-rendered frame with the upper green-stroke highlight in motion.
- `.qa/side-by-side-motto-shine.png` — normalized source/implementation full-view comparison.
- `.qa/focused-motto-shine.png` — focused source/lower-shine/upper-shine comparison.
- `.qa/design-qa-wine-90.png` — browser-rendered implementation after the 10% Wine reduction.
- `.qa/side-by-side-wine-90.png` — normalized source/implementation comparison for the latest header update.
- `.qa/focused-wine-90.png` — enlarged before/after comparison of the supplied Wine signature asset.
- `.qa/design-qa-home-inactive-text.png` — browser-rendered works-hover state after normalizing home to the inactive text style.
- `.qa/focused-home-inactive-text.png` — user target/implementation comparison for the works-hover navigation state.
- `.qa/motto-stroke-shine-upper.png` — browser-rendered upper-stroke highlight after removing rectangular clipping.
- `.qa/motto-stroke-shine-lower.png` — browser-rendered lower-stroke highlight after removing rectangular clipping.
- `.qa/focused-motto-stroke-shaped-shine.png` — user-reported rectangle artifact compared with the corrected upper/lower stroke-shaped highlights.
- `.qa/motto-opposed-float-mid.png` — browser-rendered mid-flight state with the upper stroke above and lower stroke below their fixed base positions.
- `.qa/motto-opposed-float-arrival.png` — browser-rendered arrival state with both floating highlights aligned to the fixed base strokes.
- `.qa/focused-motto-opposed-float.png` — enlarged mid-flight/arrival comparison for the latest motion direction.
- `.qa/design-qa-signature-baseline.png` — browser-rendered signature after equalizing the two support-copy sizes and lowering the cursive Wine mark.
- `.qa/focused-signature-baseline.png` — enlarged before/after comparison of signature typography, leading, and vertical alignment.
- `.qa/motto-restored-sweep-upper.png` — browser-rendered restored sweep with the upper brush-shaped highlight active.
- `.qa/motto-restored-sweep-lower.png` — browser-rendered restored sweep with the lower brush-shaped highlight active.
- `.qa/focused-motto-restored-sweep.png` — enlarged two-state comparison of the restored non-rectangular sweep.
- `.qa/design-qa-home-no-gap-flash.png` — browser-rendered about-hover state after extending the inactive-home state across the whole nav region.
- `.qa/focused-home-no-gap-flash.png` — reported/fixed navigation-state comparison.
- Browser CSS viewport: 1422×800; screenshot pixels: 1422×800; density normalization: 1:1 after source downsampling.
- Console warnings/errors: none.

**Findings**

- No remaining P0/P1/P2 mismatch in the annotated regions, including the new mosaic-motion request.
- [P3] The supplied raster signature has slight antialiasing differences from the full-page JPEG reference after brightness enhancement; acceptable at the target display size.
- [Resolved P2] Video 1 is now cropped to its useful 476×368 region and keyed with a two-texture WebGL background-difference pass. The orange plate, black pillar bars, and rectangular edge residue are removed while the moving dark details remain visible. Video 2 supplies the top-center scale and overlap reference.
- [Superseded alignment] Both mosaic groups first moved down by `.55` cell, but the left visible center still remained above the right group.
- [Resolved P2] The left mosaic now uses the right group's vertical reference and a map-specific optical correction. At 1384×900 its visible center is `467.44px`, versus `466.98px` on the right—a residual center difference of only `0.46px` while both staggered animations are running.
- [Superseded alignment] Both signature support rows first moved upward to y=`90.30px` and `105.91px`, but the W visible bottom still remained lower than the Chinese mark.
- [Resolved P2] Source-pixel bounds now drive the header alignment. At 1384×898 the Chinese mark visible bottom is `80.937px` and the W visible bottom is `80.950px`, leaving only `0.013px` residual difference. Both support rows moved with the signature group and now share `rgb(119,121,122)`, opacity `.4`, and `11.996px` size.
- [Superseded motion] The motto highlight layers were previously constrained to the supplied stroke silhouettes.
- [Resolved P2] The user requested removal of the motto motion. Both animated highlight nodes and their keyframes are deleted; only the static supplied `motto.png` remains.

**Comparison history**

- [Resolved P2] Navigation links used unequal widths and home remained selected while another item was hovered. All four links now measure 100.97 px in the QA viewport. With works hovered, home image opacity is `0`, normal home text opacity is `1`, and works alone receives the selected blur treatment.
- [Resolved P2] The original `home.png` contained a large transparent canvas and was vertically scaled during state changes, producing visible baseline drift and a transient jump. A tightly cropped source asset now uses a fixed slot with no transform or opacity transition. Before/after hover measurements return byte-identical link rectangles; the immediate switch state is image opacity `0`, text opacity `1` with no intermediate frame.
- [Resolved P2] Chinese logo was undersized. Its slot increased from 15vw to 19.5vw and now measures 277.33×39.62 px in the QA viewport.
- [Resolved P2] English signature was undersized and dim. Its slot increased to 24.5vw × 12.5vh, with brighter white outline contrast and a restrained one-pixel glow.
- Earlier supplied-asset fixes remain intact: joined lower wordmark, exact motto light strokes, exact scan-line title, and home selected-state image.
- [Resolved P2] Both mosaic groups were static. Their original cell count, positions, dimensions, grayscale values, and outline styling remain unchanged; only per-cell opacity, brightness, and sub-pixel transform now animate on staggered 4.1–5.8 second cycles. A 700 ms two-frame browser comparison changed all 16 sampled cells while both mosaic container rectangles remained byte-identical.
- [Resolved P2] The Chinese and English header marks visually crowded one another. The signature slot moved right without changing either supplied logo asset; thresholded browser evidence now shows a 31px clear visual interval between the two marks at 1422px width.
- [Resolved P2] Navigation labels resolved to approximately 32px from a viewport-relative clamp. The user-requested trial size is now exactly `20px`, and every link uses the same fixed 100×44px slot.
- [Resolved P2] The selected home raster was stretched into a square, causing optical shrinkage and state-switch instability. It now preserves its source aspect ratio at 128px wide, remains absolutely centered in the fixed slot, and both its image and fallback label have zero-duration transitions. Link and image geometry therefore remain independent of selected-state visibility.
- [Resolved P2] The wine-word dot was embedded in the full raster and could not move independently. It was losslessly separated into `wine-word-base.png` and `wine-dot.png`; the base wordmark remains unchanged while the real supplied dot asset receives a short diagonal float and fading afterglow. Two browser frames 650ms apart returned different transforms.
- [Superseded trial] The Chinese header mark was temporarily tested at 9vw (approximately 14–16px visible height at 1682px), then restored after the user found it too small.
- [Resolved P2] The signature raster was forced from its source 1.82587 aspect ratio into a wide 3.68 slot, flattening and stretching the W. The rendered image now measures the same 1.82587 aspect ratio as the source; its taller natural canvas lets the W extend downward like the annotated reference.
- [Resolved P2] The two English support rows needed +2px type and tighter leading. Both sizes now use their prior responsive values plus 2px, and the browser-measured gap between row boxes is 2.24px instead of the earlier loose percentage spacing.
- [Resolved P2] The latest tab trial requested 22px. Computed browser evidence confirms all four labels inherit exactly `22px` while their four 100×44px slots remain identical and the home image/text transitions remain `0s`.
- [Resolved P2] The 9vw Chinese-mark trial was rejected as too small. The mark is restored to the prior 19.5vw size, and the proportional English signature is positioned at 22vw so the two visible marks keep a safe interval instead of overlapping.
- [Resolved P2] The lower wordmark inherited a `scaleY(1.14)` transform, producing the annotated vertical stretch. That transform is removed. Browser geometry now reports a rendered aspect ratio of `1.785939`, matching the supplied 734×411 source ratio of `1.785888` within rounding; the independently animated dot still changes transform across frames.
- [Resolved P2] The visible content inside the selected-home PNG sat above the other labels because its transparent top/bottom padding is asymmetric. The fixed image slot now receives a 3px downward optical offset. Using the asset's measured alpha bounds, its visible center is `71.384px` versus the other labels' `71.589px` center at the QA viewport—a residual difference of only `0.205px`. All four link rectangles remain identical and both home-state transitions remain `0s`.
- [Superseded state treatment] The recording-driven fix temporarily kept `nav-home-tight.png` visible in every state to remove perceived switching. The latest user annotation explicitly requires home to match the normal inactive text style whenever another tab is selected; the fixed link slot remains, but visibility now switches instantly between the selected raster and the inactive text layer.
- [Resolved P2] The header rules were approximated as two lines, placing their overlap too far from the Wine signature and omitting the second line under home. Direct source-image measurement found three two-pixel source runs: `(x 240–1555, y 282–283)`, `(x 1410–3599, y 304–305)`, and `(x 2284–3599, y 326–327)` in the 3840×2160 design. These are implemented as 6.25%–40.5% at 13.06vh, 36.72%–93.75% at 14.07vh, and 59.48%–93.75% at 15.09vh. Browser geometry at 1422×800 matches the normalized targets within 0.14px and restores the two-line pair beneath the navigation.
- [Resolved P2] The English support copy began at 21.5% of the signature slot, visually running beneath the large W. It now begins at 38%, immediately after the W's right edge, while both rows gain the requested additional 1px (`calc(.86vw + 3px)` and `calc(.65vw + 3px)`). At the 1280×720 browser viewport both rows share x=`400.76px`; computed sizes are `14.008px` and `11.32px`, and their 0.52px box gap preserves the intentionally tight leading.
- [Resolved P2] The latest annotation requests 24px navigation text. The shared nav size is now exactly `24px`; browser evidence confirms every label uses a `24px` line-height while all four link slots remain identical at `100×44px` and y=`44.63px`, preserving the no-jump alignment model.
- [Superseded motion treatment] The first motto-light version used horizontal clip-path bands over a green-only overlay. Although the color stayed on the original artwork, the hard reveal edge produced the user-reported rectangular flash and has been removed.
- [Resolved P2] The latest annotation requests the supplied cursive Wine mark approximately 10% smaller. The raster now renders at `90%` of its prior width while retaining its source aspect ratio and fixed left/top anchor. Browser geometry changed from `313.594×171.75px` to `282.234×154.57px`, an exact 10% reduction. The support-copy sizes remain unchanged; their shared start moved proportionally from 38% to 34.2% so it still begins at the W's right edge.
- [Resolved P2] During works/about/class hover or keyboard focus, home remained as a dimmed version of the selected raster and therefore did not match the other inactive tabs. It now switches to the same HTML text treatment: `24px`, `24px` line-height, normal style, `rgb(83,85,87)`, and no filter. Works-hover browser evidence confirms home image opacity `0`, text opacity `1`, while the unchanged home link rectangle remains exactly `100×44px` at x=`719.375`, y=`44.633`.
- [Resolved P2] The motto highlight had a rectangular leading edge. The green source pixels are now split into `motto-green-upper.png` and `motto-green-lower.png`, each retaining only one original brush-stroke silhouette. The animation uses shape-preserving opacity, brightness, glow, and 1–2px diagonal translation—no clip-path or rectangular mask. Browser evidence confirms computed `clip-path: none`, peak opacities `.815`/`.811`, and 15/13 distinct transforms across the sampled cycle.
- [Superseded motion direction] The initial shape-preserving version only drifted each highlight a few pixels along its own stroke. The user selected a more spatial entrance: upper from above and lower from below.
- [Resolved P2] The base `motto.png` remains completely static, including both original green strokes. Separate stroke-shaped highlights now enter almost simultaneously on a 5.4s rhythm: upper from above, lower from below, with a `.16s` offset. A browser mid-flight frame measured upper y-translation `-16.54px` and lower y-translation `+19.51px` at similar opacity, while the arrival frame aligns both to their base strokes before fading. Both layers retain `clip-path: none`.
- [Superseded motion direction] The upper-from-above/lower-from-below entrance was tested, then explicitly rejected in favor of the preceding local sweep treatment.
- [Resolved P2] The motto motion is restored to the prior 4.8s brush-shaped light sweep. The fixed base artwork does not move; the two extracted upper/lower stroke layers brighten and drift only 1–2px along their original stroke direction before fading. Browser sampling captured both active states, measured peak opacities `.817`/`.849`, 20/17 distinct transforms, the separate upper/lower PNG sources, and computed `clip-path: none` throughout—so no rectangular mask can appear.
- [Resolved P1] Home could flash its selected raster while the pointer crossed the 26.875px gaps between works/about/class because the inactive rule depended on a child link's `:hover`. The inactive-home state now remains active for the entire hovered nav container, with a higher-specificity override only when home itself is hovered. Immediate browser samples at works, both inter-tab gaps, about, and class all returned image opacity `0` and text opacity `1`; moving to home returned image opacity `1` and text opacity `0`. The 100×44px link rectangles and zero-duration layer transitions remain unchanged.
- [Resolved P2] The two English support rows used visibly different sizes and retained more leading than requested. Both now compute to exactly `11.32px` at the 1280×720 QA viewport. Their boxes are 14px high and begin at y=`62.18px` and y=`76.95px`, leaving a tight `0.77px` visual gap. The supplied cursive Wine raster moved down by `1.35vw` (`17.28px` at this viewport); using its source alpha bounds, the visible W bottom lands within approximately 2px of the hooked-line bottom.
- [Resolved P2] The left and right bracket groups read as connected repeated rails instead of four independent symbols. Each side now uses four separately positioned bracket strokes with progressively shorter horizontal terminals and source-like dark-to-white emphasis; the right group is a strict mirror of the left.
- [Resolved P2] Both mosaic clusters were too small and sat above the bracket centerline. Cell scale increased from `.92vw` to `1.08vw` (+17.4%) without changing the existing cell map or animation. Their top offsets now derive from each cluster's actual occupied height. At the 1280×720 QA viewport the bracket center is `368.27px`; visible mosaic centers are `367.10px` left and `367.04px` right, a maximum residual difference of `1.23px` while the staggered animation is running.
- [Resolved P2] The supplied Wine raster completely covered the first English support row. Its true alpha boundary ends at source y=`118/201`; at the final browser viewport this maps to y=`82.84px`. The first row now begins at y=`84.64px`, leaving `1.80px` clear space below the visible Wine pixels, while the second row retains a compact `0.41px` box gap.
- [Resolved P2] The left mosaic overlapped the WINE title and sat too far right. Its visible left edge now measures `197.30px`, versus the supplied WINE title's calculated visible W edge at `197.63px` (0.33px difference). Its visible bottom is `457.27px`, leaving `11.12px` before the title's visible top at `468.39px`.
- [Resolved P2] The right mosaic used a different optical gap to its inner bright bracket. The final left gap is `22.62px`; the mirrored right gap is `22.30px`, a residual difference of `0.32px` during the running cell animation.
- [Resolved P2] The animated motto highlights expanded beyond the original green brush silhouettes. Both extracted stroke overlays now remain below source scale (`.94–.965`), use sub-pixel translations within the shrink margin, and remove all non-zero drop-shadow spread. The sampled peak frame reached opacity `.689`, scale `.9647`, and brightness `1.572` while staying inside the supplied transparent stroke asset.
- [Resolved P2] After the support copy moved below Wine, the existing source-measured rule positions crossed the second English row at the annotated 1384×900 viewport. The two signature-adjacent rule levels now sit at y=`139.50px` and y=`153.90px`; the second row ends at y=`130.44px`, leaving `9.06px` and `23.46px` of clear vertical space. The support-copy geometry remains unchanged, so its prior Wine clearance is preserved.

**Required fidelity surfaces**

- Fonts and typography: four navigation labels share the latest user-requested 24px size and 24px line-height, fixed link box, and optical weight; the selected-home raster retains the same fixed slot. Selected state changes treatment, not geometry. Both signature support rows now share the same responsive `calc(.65vw + 3px)` size, start at the W's right edge, and use sub-pixel-tight leading.
- Spacing and layout: equal 100×44px navigation slots preserve stable positions when selection moves; the restored 19.5vw Chinese mark and proportional signature retain a safe visual interval. The three header rules now use source-measured normalized endpoints and vertical offsets. Mosaic container geometry is stable across animation frames.
- Colors and visual tokens: inactive tabs remain `rgb(83,85,87)`; selected text uses the intended soft white blur. Mosaic animation preserves the existing gray palette and modulates only local opacity/brightness.
- Image quality and asset fidelity: supplied PNG assets remain in use; the signature and lower wordmark now preserve their exact source aspect ratios without replacement or redrawing. The animated green dot is a lossless crop of the supplied wordmark rather than a CSS-drawn replacement.
- Header image scale: the supplied Wine raster remains unmodified and is displayed at the requested 90% scale with no stretching; support text retains its previously approved sizes.
- Motion fidelity: the fixed motto—including its original green marks—never moves. Two extracted, brush-shaped highlight layers perform a restrained local sweep along their own silhouettes, brighten, then fade; base typography and punctuation stay unchanged.
- Decorative fidelity: bracket marks are visually separate, mirrored, and progressively emphasized; enlarged mosaic cells retain their original layouts and motion while aligning to the bracket centerline.
- Signature copy fidelity: both support rows preserve their approved size and horizontal anchor while clearing the visible Wine raster boundary.
- Copy and content: home, works, about, class and logo copy are unchanged.

**Primary interactions tested**

- Default home selected state.
- Works hover selected state.
- Automatic home fallback to unselected text while works is hovered.
- Equal link geometry across all four tabs.
- Immediate state switch with zero geometry delta and no opacity interpolation.
- Recording-derived tab sequence: works/home/works/about/home. The fixed 100×44px slot prevents layout movement while the selected raster and normal inactive text switch with zero-duration visibility changes.
- Pointer traversal sequence: works → gap → about → gap → class. Home remained the 24px inactive text at every sample, including both gaps, and restored the selected raster only over home.
- Left/right mosaic staggered animation across multiple sampled frames.
- Reduced-motion fallback disables mosaic animation without changing its static design.
- Wine-dot transform changes across sampled frames; reduced-motion restores the original static dot and removes the trail.
- Upper/lower motto highlight opacity and short stroke-direction transforms change across sampled frames; computed clip-path remains `none`; reduced-motion hides both animated overlays while leaving the fixed original strokes visible.
- Page-load subject playback: video reaches `readyState 4`, remains unpaused, advances from 0.95s to 2.17s across captured frames, and drives a 476×368 WebGL canvas without blocking pointer input.

**Implementation checklist**

- Equal tab sizing: passed.
- Tab baseline alignment and no-jump switching: passed.
- Mutually exclusive hover selection: passed.
- Chinese logo scale: passed.
- English signature scale/brightness: passed.
- Mosaic motion with unchanged base design and stable layout: passed.
- Header logo spacing: passed.
- 22px equal-slot navigation: passed.
- Enlarged home selected state with zero-duration, geometry-independent switching: passed.
- Supplied wine-dot asset animation: passed.
- Chinese header mark restored to the prior 19.5vw scale: passed.
- Signature source-ratio/W alignment and tightened support copy: passed.
- Lower wordmark source aspect ratio with no vertical stretch: passed.
- Selected-home horizontal alignment and stable state geometry: passed.
- Home selected raster / inactive 24px text switching inside a fixed slot: passed.
- Source-measured Wine overlap and two-line navigation rules: passed.
- English support-copy +1px and right-of-W alignment: passed.
- 24px equal-slot navigation alignment: passed.
- Restored brush-shaped light sweep with no rectangular mask: passed.
- Wine signature exact 10% proportional reduction: passed.
- Equal signature support-copy sizing, tighter leading, and lowered W baseline: passed.
- Home inactive state matches works/about/class typography: passed.
- Home does not flash while crossing inter-tab gaps: passed.
- Left/right bracket symbols separated and mirrored: passed.
- Left/right mosaic scale and vertical-center alignment: passed.
- Left mosaic W alignment and title clearance: passed.
- Mirrored mosaic-to-bracket spacing: passed.
- Motto highlight contained inside supplied green strokes: passed.
- Signature support-copy visibility below Wine: passed.
- Signature support-copy clearance from header rules: passed.
- Browser console: passed.
- Top subject video autoplay, loop, transparent matte, and 1200×675 placement: passed.
- Latest mosaic center alignment, pixel-edge header alignment, equal support-copy styling, and static motto: passed.
- New 3184×1792 black-background subject video: WebGL black matte removes the plate at two sampled playback times, preserves autoplay/loop, and leaves the existing page geometry unchanged: passed.
- Subject loop seam: native loop replaced by a measured matched-pose restart at 0.50s; repeated runtime samples show continuous playback with no crossfade or double-exposure layer: passed.

final result: passed
